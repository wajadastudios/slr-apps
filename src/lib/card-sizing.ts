/**
 * Shared width for every "contoh aplikasi" preview card — the hero card
 * (`AppPreviewCard`) and the `#contoh-aplikasi` gallery's mobile carousel
 * cards (`AppGallery`) — so they render at the same size instead of each
 * component picking its own arbitrary value.
 */
export const APP_CARD_WIDTH_CLASS = "w-[88vw] max-w-[380px]";
